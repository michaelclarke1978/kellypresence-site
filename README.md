# kellypresence-site

Published static site for **www.kellypresence.ai**, served via GitHub Pages.

This repository holds only the public web assets (`index.html`, the reference
image, `CNAME`, `.nojekyll`). It is the deploy target for GitHub Pages; the
working source and tooling are maintained elsewhere and synced here on deploy.
